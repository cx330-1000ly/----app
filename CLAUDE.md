# 记账APP —— 项目说明与产品文档

> 本文件是项目的「说明书」：产品需求、技术决策、协作规则都在这里。
> Claude Code 每次开始工作前先读本文件；内容变化时及时更新。

## 一、项目简介

- **产品名称**：记账APP（原「黑马记账」，2026-09-16 改名；内部标识名 heima-jizhang 保持不变，数据位置不受影响）
- **产品定位**：运行在 Windows 和 Mac 电脑上的个人记账软件，记录每一笔收入和支出（人民币），支持二级分类，帮助用户清楚掌握「钱花在了哪里」。
- **目标用户**：需要记录日常开销的个人用户（用户本人，不懂编程）
- **目标平台**：Windows 10/11、macOS
- **货币单位**：人民币（元，精确到分）

## 二、功能需求（v1.0 标准版，已与用户确认）

1. **记一笔**：记录一笔收支，字段包括——类型（支出/收入）、金额（元，两位小数）、日期、一级分类、二级分类、备注（选填）
2. **二级分类体系**：内置一套分类（见第三节），用户可在 App 内自行增删改分类
3. **明细列表**：按时间倒序展示所有记录；支持按备注/金额/分类搜索；支持按月份、类型、分类筛选
4. **编辑/删除**：任何一条记录可修改或删除
5. **按月统计**：本月总收入、总支出、结余；分类占比图表；近几个月支出趋势
6. **数据安全**：数据保存在本机数据库（SQLite）；提供数据文件位置说明与备份指引（Excel 导出为后续候选功能）
7. **休闲小游戏**：内置贪吃蛇（键盘方向键控制，工作间隙放松）；与记账数据完全隔离，得分不保存（2026-09-16 用户确认新增，开发于 feature/snake-game 分支，尚未合并主线）

### 后续候选功能（用户未确认，暂不开发）
预算管理、Excel 导出、深色模式、云同步

## 三、分类体系设计（二级）

### 支出分类（一级大类 → 二级小类）

| 一级大类 | 二级小类 |
|---|---|
| 餐饮 | 早餐、午餐、晚餐、外卖、零食饮料、咖啡奶茶、聚餐请客 |
| 交通 | 公交地铁、打车、共享单车、加油充电、停车费、长途出行 |
| 购物 | 服饰鞋包、日用品、数码电器、美妆护肤、母婴用品、其他购物 |
| 居住 | 房租/房贷、水电燃气、物业费、宽带话费、家居维修、家具家纺 |
| 娱乐 | 电影演出、游戏、运动健身、旅行、KTV/酒吧、其他娱乐 |
| 医疗 | 门诊、购药、住院、体检、保健 |
| 教育 | 书籍、课程培训、学费、文具 |
| 人情 | 红包礼金、送礼 |
| 其他 | 其他支出 |

### 收入分类（一级大类 → 二级小类）

| 一级大类 | 二级小类 |
|---|---|
| 工资 | 基本工资、奖金、补贴 |
| 理财 | 利息、基金股票收益 |
| 兼职 | 副业收入 |
| 红包 | 收到的红包礼金 |
| 其他 | 其他收入 |

> 以上为内置初始分类，用户可在 App 内自行增删改。

## 四、技术方案（已确认）

| 决策项 | 结论 | 说明 |
|---|---|---|
| 应用框架 | Electron | 技术最成熟、跨平台最稳、AI 辅助开发最顺畅 |
| 界面框架 | Vue 3 | 中文资料最多、语法直观，适合表单+列表界面 |
| 代码语言 | TypeScript | 写代码时自动检查错误，程序更稳定 |
| 搭建工具 | electron-vite | 社区主流，启动快、改代码即时生效 |
| 数据存储 | 本地 SQLite 数据库 | 仅本机存储，无账号体系，无服务器 |
| 数据库实现 | sql.js | 纯 JS 实现的 SQLite，安装零网络风险，每次操作立即写盘保证数据安全 |
| 界面布局 | 左侧导航 | 记一笔 / 明细 / 统计 / 小游戏 / 设置 五个页面 |
| 配色风格 | 清新绿 | 主色翠绿系；金额显示：支出红、收入绿 |
| 图表库 | ECharts | 国内最流行、中文文档全 |
| 开发平台 | Windows 11（用户电脑） | Mac 安装包需在 Mac 电脑或云端打包，届时列方案确认 |

### 开发环境备忘（重要，勿删）

- **国内镜像源**：项目内 [.npmrc](.npmrc) 已配置 Electron 本体及打包工具从 npmmirror 镜像下载（GitHub 在国内连不上）。重装依赖时自动生效。
- **启动软件的注意事项**：Claude Code 终端环境会自动注入 `ELECTRON_RUN_AS_NODE=1`，会把 Electron 压成命令行模式、导致窗口打不开。**启动开发版时必须先关闭它**：`unset ELECTRON_RUN_AS_NODE && npm run dev`。用户双击打包后的正式版不受影响。
- **启动方式**：开发版 = `unset ELECTRON_RUN_AS_NODE && npm run dev`；打包 = `npm run build:win`（届时再验证）。

### 备选技术栈（已对比、未选用，供回顾）
- **Tauri 2**：更轻量（安装包 5~10MB），但需 Rust 编译环境、排查难度高
- **Flutter 桌面版**：动画流畅、将来可复用做手机版，但桌面支持较新、需装几 GB 的 C++ 工具链
- **.NET MAUI（C#）**：Windows 原生体验好，但 Mac 端弱、生态小

## 五、协作规则（★ 重要，整个项目期间必须遵守）

用户是不懂编程的初次使用者，因此约定：

1. **技术决策必须由用户拍板**：任何涉及技术的事情（选框架、选库、选实现方式、环境配置、打包方式等），Claude 必须先列出 2 个以上方案，用通俗语言解释每个方案的优缺点，并给出推荐方案及理由，由用户选择决定。
2. **不得擅自替用户做技术决定**：未经用户确认的方案，不得直接实施。
3. **语言**：所有沟通用中文，解释要通俗，避免专业术语；无法避免时必须附带解释。
4. **需求确认**：用户需求可能口语化，动手开发前先复述确认理解无误。
5. **开发透明**：每个新功能开发前，先简要说明要做什么、会影响什么，得到用户确认后再开始。
6. **文档同步**：每次重大决策或需求变化，及时更新本文件。

## 六、项目状态

- 2026-09-14：项目启动，产品文档建立；已确认技术栈（Electron）、存储（本地 SQLite）、功能范围（v1 标准版）、收支范围（支出+收入）
- 2026-09-14：已确认界面框架（Vue 3）、代码语言（TypeScript）、搭建工具（electron-vite）；开发环境就绪（Node.js v24.16.0）
- 2026-09-14：项目骨架搭建完成，依赖安装完成（走国内镜像），开发版运行验证通过（窗口正常打开）
- 2026-09-14：v1 功能全部开发完成（记一笔/明细/统计/设置），用户实测通过；修复金额输入框类型 bug（type=number 返回数字导致保存中断）
- 2026-09-14：Windows 安装包打包成功：dist/黑马记账-v1.0.0-安装包.exe（约 111MB）；应用图标已替换为专属图标（绿色圆角方块+¥）
- 打包命令：`unset ELECTRON_RUN_AS_NODE && npm run build:win`（走国内镜像下载打包工具）
- 2026-09-14：用户已安装正式版并验证通过（桌面图标打开正常）。开发版与正式版共用同一账本位置（%APPDATA%/heima-jizhang），数据无缝衔接
- 2026-09-15：应需求确认全局记忆内容（用户协作偏好记忆，与账本数据库无关）；向用户说明账本数据库两张表（records / categories）的字段结构
- 2026-09-16：产品改名「黑马记账」→「记账APP」（仅显示名：窗口标题/界面Logo/设置页/备份文件名/安装包名；内部标识 heima-jizhang 与数据文件夹保持原样，账本数据零影响）
- 2026-09-16：用户确认新增「小游戏」页面（贪吃蛇，键盘方向键控制、开始按钮、重新开始），开发于独立分支 feature/snake-game，尚未合并主线；代码审查后修复两处 bug：棋盘被蛇占满时程序卡死（改为通关结束）、游戏卡片背景变量名错误导致透明
- 下一步：Mac 版打包方案（需在 Mac 上打包，届时列方案确认）；手机版需作为新项目另议
